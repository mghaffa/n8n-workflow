# name: xAI Probe v1 (Only API)

# on:
#   workflow_dispatch:
#     inputs:
#       XAI_MODEL:
#         description: "xAI model to probe"
#         required: false
#         default: "grok-4-fast-reasoning"

# jobs:
#   probe:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#       - name: Probe xAI
#         env:
#           XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
#           XAI_MODEL:   ${{ inputs.XAI_MODEL }}
#         run: node runner/xai_probe_v1.mjs

name: Probe X.AI (Grok)
  env:
    XAI_API_KEY: ${{ secrets.XAI_API_KEY }}   # must be an X.AI key (NOT gsk_)
  run: |
    curl -sS https://api.x.ai/v1/chat/completions \
      -H "Authorization: Bearer $XAI_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "model":"grok-4-fast-reasoning",
        "messages":[{"role":"user","content":"hello"}]
      }'


